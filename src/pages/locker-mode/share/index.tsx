import * as Styled from './style';

import PageTemplate from '@/components/common/PageTamplate';

const SharePage = () => {
  return (
    <PageTemplate>
      <Styled.Root>쉐어</Styled.Root>
    </PageTemplate>
  );
};

export default SharePage;
